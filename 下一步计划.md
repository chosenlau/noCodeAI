### 1 持久化与记忆：修改baseagent依赖的redistory，让set进行LRU并调用summary总结，但是我不是很清楚什么记录才应该持久化，什么时候才放mysql，也不是很清楚checkpointstore里面的信息包含什么，包含agent的turn对话吗，包含agent的思考吗。

### 2 代码返回：想在workflowcontext里面加一个codecontent，在质检成功后把代码内容赋值进去，方便后面将代码返回给前端，现在质检节点的read是生成一个文件给ai质检，我想在这个节点这里写个函数把代码变成json格式然后反倒 workflowctx的codecontent里，这样可行吗

### 3 中断情况处理： 现在整个流程都没有听取ctx.done也没有主动cancel，后面要加入各种中断情况的对应处理：
#### 情况1：用户主动点击取消
#### 情况2：用户关闭浏览器标签页
#### 情况3：网络闪断（临时断开，3秒后恢复）

### 4 图片搜索工具配置与全流程测试

### 5 logic层service层handler层更新增加功能（app部署与历史记录维护等）

### 6 滑动窗口限流中间件
