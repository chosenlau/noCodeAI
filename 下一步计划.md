# 下一步改造计划

## 总体目标

建立完整的代码生成、结构化返回、浏览器预览、任务恢复和正式部署链路：

```text
代码生成 -> 代码质检 -> 结构化返回 -> 前端预览 -> 用户确认 -> 正式部署
```

预览和部署必须分离。预览用于验证生成结果，用户主动点击部署后才执行正式发布。

## 一、代码结构化返回

优先改造 `WorkflowContext`，增加生成文件内容：

```go
CodeContent map[string]string
```

示例：

```json
{
  "/index.html": "...",
  "/src/App.vue": "...",
  "/src/main.js": "...",
  "/package.json": "..."
}
```

质检节点当前会读取文件并拼接文本给 AI。应将逻辑拆分为：

```text
读取代码文件
  -> 得到 map[string]string
  -> 保存到 WorkflowContext.CodeContent
  -> 拼接成质检消息
  -> 调用质检 Agent
```

不要在质检成功后重新读取文件。质检失败重试时，也应该保留当前代码内容。

需要过滤：

- `node_modules`
- `dist`
- `.git`
- 二进制文件
- 与代码无关的大型文件

需要测试：

- HTML 单文件
- multifile
- Vue 项目
- 空目录
- 文件读取失败
- 路径安全和相对路径转换

## 二、前端结构化预览

前端逐步从解析聊天文本改为接收结构化项目文件：

```json
{
  "type": "vue_project",
  "files": {
    "/src/App.vue": "..."
  }
}
```

预览策略：

```text
HTML       -> iframe + srcDoc
multi_file -> 文件资源预览或 Sandpack
vue_project -> Sandpack Vue/Vite 模板
```

聊天流和代码结果分开：

```text
聊天流：AI 回复、节点步骤、工具调用摘要
代码结果：files、编辑器内容、预览内容
```

预览阶段可以由浏览器沙盒承担依赖加载和编译，但不能把 Sandpack 视为正式部署。

## 三、Workflow 中断和取消

当前阶段只实现**用户主动取消任务**，不实现断点恢复。

服务端作为任务执行主体，应该保证：

```text
用户断开浏览器或网络连接
  -> 服务端任务继续执行
  -> 任务结果保存到服务端
  -> 用户重新进入页面后查询结果
```

用户主动点击取消时：

```text
用户点击取消
  -> 服务端取消当前 workflow
  -> 当前 Agent、LLM 请求和工具调用收到取消信号
  -> 任务状态变为 canceled
  -> 不保存可恢复 checkpoint
  -> 用户下次重新生成时从头开始
```

当前阶段暂不实现：

- 从中断节点恢复
- Agent 内部 checkpoint 恢复
- workflow checkpoint 恢复
- 网络断开后的自动续跑
- 从 token 或工具调用中间位置继续

### 3.1 Context 传递

确保同一个可取消的 `context.Context` 贯穿：

```text
handler -> logic -> workflow -> agent -> model/tool
```

但不能直接使用 HTTP 请求的 context 作为任务生命周期 context。HTTP 连接断开后，任务 context 不应被自动取消。

建议由任务服务创建独立任务 context：

```go
taskCtx, cancel := context.WithCancel(context.Background())
```

并按 `runID` 保存取消函数：

```text
runID -> cancel()
```

用户点击取消时，服务端根据 `runID` 调用对应的 cancel。

### 3.2 节点和 Agent 的取消检查

节点开始、文件遍历、工具执行和重试循环中检查：

```go
select {
case <-ctx.Done():
    return nil, ctx.Err()
default:
}
```

LLM 请求和工具实现必须使用传入的 context，不能重新创建 `context.Background()` 覆盖任务 context。

### 3.3 任务状态

当前阶段至少维护：

```text
pending
running
completed
failed
canceled
```

建议每个任务包含：

```text
runID
appID
status
currentStep
errorMessage
startedAt
completedAt
```

用户断开后任务继续执行，因此任务状态不能只存在于内存。后续应将状态保存到 Redis 或 MySQL。

### 3.4 取消后的清理策略

用户取消后需要明确清理行为：

- 删除本次未完成的临时代码目录，或标记为临时目录
- 不覆盖上一次已完成的代码版本
- 关闭 LLM 和工具流
- 释放任务 cancel 函数
- 更新任务状态为 `canceled`
- 保存取消原因和取消时间

如果代码生成过程会覆盖同一个目录，应改为使用：

```text
tmp/code_output/{appID}/{runID}/
```

任务完成后再将成功版本绑定为当前版本，避免取消任务破坏已有结果。

### 3.5 当前阶段验收标准

需要补充测试：

1. workflow 正常完成后状态为 `completed`
2. 用户断���前���连接，服务端任务仍完成
3. 用户主动取消后 workflow 返回 `context.Canceled`
4. 取消信号能传到 Agent、LLM 和工具
5. 取消后不继续执行后续节点
6. 取消后任务状态为 `canceled`
7. 同一个应用的旧代码不会被取消任务破坏
8. 任务完成或取消后，内存中的 cancel 函数被释放

CheckpointStore 和断点恢复保留到后续阶段。当前阶段的目标是：

```text
任务能稳定完成
用户能主动取消
网络断开不影响服务端任务
取消后下次重新从头执行
```

## 四、持久化与记忆

在代码返回和中断模型稳定后，再改造 Redis、summary 和 CheckpointStore。

数据职责建议：

```text
Redis：
  短期会话、最近上下文、流式任务状态、临时缓存

MySQL：
  应用信息、用户消息、AI 回复、生成文件元数据、部署记录

CheckpointStore：
  workflow/agent 恢复需要的中间状态
```

需要确认当前 CheckpointStore 实际保存内容，重点检查：

- workflow 节点状态
- 消息历史
- tool call 状态
- 重试次数
- WorkflowContext
- agent turn 信息

不建议默认持久化完整内部思考过程。优先保存：

- 用户消息
- AI 对外输出
- 工具调用摘要
- 文件变更
- 节点状态
- 错误信息

需要明确：

- 什么时候触发 summary
- summary 后保留哪些原始消息
- Redis 淘汰后如何从 MySQL 恢复
- checkpoint 失效后的降级策略
- LRU 淘汰是否影响任务恢复

## 五、图片搜索工具配置与测试

在代码返回模型稳定后，配置并测试图片相关完整链路：

```text
图片搜索
  -> 提示词增强
  -> 图片 URL 进入代码
  -> 文件落盘
  -> 代码质检
  -> 前端预览
```

COS、Pexels、DashScope 未配置时跳过对应测试，不阻塞 HTML、multifile 和 Vue 基础链路。

## 六、构建与预览

Builder 只负责验证和生成构建产物，不负责正式部署：

```text
quality_check
  -> project_builder
  -> BuildResultDir
  -> workflow 结束
```

构建状态和部署状态分开：

```text
BuildStatus:
  pending / success / failed

DeployStatus:
  not_deployed / deploying / deployed / failed
```

Vue 预览可以使用浏览器 Sandpack，正式部署前仍需要服务端或 CI 重新构建验证。

## 七、应用部署

部署必须由用户主动触发，不应由代码生成 workflow 自动执行。

推荐调用链：

```text
handler
  -> appService.DeployApp
      -> deployService.Deploy
      -> 更新部署状态、DeployKey、DeployedTime
```

职责划分：

```text
internal/router：
  只注册 HTTP 路由

internal/handler：
  参数解析、鉴权、调用 service、返回响应

internal/logic：
  部署业务编排和权限校验

internal/core/deploy：
  复制 dist、上传对象存储或执行具体部署
```

部署前提：

- 代码已经生成
- 质检通过
- 项目构建成功
- 用户主动点击部署

建议接口：

```text
POST /app/{appId}/deploy
GET  /app/{appId}/preview
GET  /app/{appId}/deploy-status
```

`appId` 用于内部关联，`DeployKey` 用于公开访问地址。不要直接把数据库 ID 作为公开部署路径。

## 八、滑动窗口限流

最后实现限流，优先限制高成本操作：

- LLM 生成
- 图片搜索
- 项目部署

限流维度需要明确：

- 用户
- 应用
- IP
- 接口类型

普通查询接口可以使用更宽松的限制。

## 推荐实施顺序

```text
1. CodeContent 文件结构
2. 质检节点复用结构化文件
3. 前端 multifile/Vue 预览
4. Workflow Context cancellation
5. 任务状态与历史记录
6. Redis、CheckpointStore、summary
7. 图片工具配置和全流程测试
8. Builder 构建状态完善
9. App 部署
10. 滑动窗口限流
```

## 每阶段完成标准

完成后请以教学的方式告诉我改了哪些地方

每个阶段至少补充：

- 单元测试
- 关键链路集成测试
- 失败和取消场景测试
- 日志和错误信息
- 前后端接口数据结构说明

真实 LLM、外部图片服务和正式部署测试应通过环境变量或配置开关控制，避免影响默认测试。
